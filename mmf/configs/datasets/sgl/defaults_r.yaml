dataset_config:
  gt_rtr:
      data_dir: ${env.data_dir}/datasets
      depth_first: false
      fast_read: false
      use_images: false
      use_features: true
      zoo_requirements:
      - gt_rtr.defaults
      images:
        train:
        - gt_rtr/defaults/images/train
        val:
        - gt_rtr/defaults/images/val
        test:
        - gt_rtr/defaults/images/test
      features:
        train:
        - gt_rtr/defaults/features/train
        val:
        - gt_rtr/defaults/features/val
        test:
        - gt_rtr/defaults/features/test
      annotations:
        train:
        - gt_rtr/defaults/annotations/imdb_train.npy
        val:
        - gt_rtr/defaults/annotations/imdb_val.npy
        test:
        - gt_rtr/defaults/annotations/imdb_test.npy
      max_features: 100
      processors:
        text_processor:
          type: vocab
          params:
            max_length: 30
            vocab:
              type: intersected
              embedding_name: glove.6B.300d
              vocab_file: gt_rtr/defaults/extras/vocabs/vocabulary_100k.txt
            preprocessor:
              type: simple_sentence
              params: {}
        answer_processor:
          type: rtr_result
          params:
            vocab_state_file: gt_rtr/defaults/extras/vocabs/state_gtrtr.txt
            vocab_object_file: gt_rtr/defaults/extras/vocabs/object_gtrtr.txt
            preprocessor:
              type: simple_word
              params: {}
        context_processor:
          type: fasttext
          params:
            download_initially: false
            max_length: 50
            model_file: wiki.en.bin

      return_features_info: false
      # Return OCR information
      use_ocr: false
      # Return spatial information of OCR tokens if present
      use_ocr_info: false
